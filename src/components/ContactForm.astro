---
---

<section id="contact-success">
  <h2>Thank you for reaching out!</h2>
  <p>Your message has been sent. I will get back to you as soon as possible.</p>
</section>

<section id="contact-failed">
  <h2>Something went wrong</h2>
  <p>Your message could not be sent. Please try again later or reach out to me via LinkedIn or GitHub.</p>
</section>

<section id="contact">
  <h2>Contact me</h2>
  <p>If you are interested in hiring me for a project or if you have any questions, feel free to reach out to me via
    the contact form below.</p>
  <div id="contact-form">
    <form action="/api/contact" method="POST">
      <input type="text" name="name" placeholder="Your name" required>
      <input type="email" name="email" placeholder="Your email" required>
      <input type="checkbox" name="contact_by_fax_only" value="1" style="display:none!important" tabindex="-1" autocomplete="new-password">
      <input type="text" name="password" style="display:none!important" tabindex="-1" autocomplete="new-password">
      <textarea name="message" rows="5" placeholder="Your message" required></textarea>
      <input type="submit" value="Send">
    </form>
  </div>
</section>

<style lang="scss">
  #contact-success,
  #contact-failed {
    display: none;

    &:target {
      display: block;
    }

    &:target ~ #contact {
      display: none;
    }
  }

  #contact-form {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding-top: 1.5em;
  }

  input, textarea {
    width: 100%;
    padding: 0.8em 0.5em;
    font-size: 1.1em;
    margin-bottom: 0.5em;
    background-color: var(--blue-green);
    color: var(--whiteish);
    border: 2px solid var(--blue-green);
  }

  input:focus, textarea:focus {
    border: 2px solid var(--copper);
  }

  input[type="submit"] {
    cursor: pointer;
    background-color: var(--copper);
    color: var(--dark-blue);
  }

  @media (hover: hover) {
    input[type="submit"]:hover {
      color: var(--whiteish);
    }
  }

  @media (min-width: 768px) {
    input[type="text"],
    input[type="email"],
    textarea {
      background-color: var(--blue-green);
    }
  }
</style>
